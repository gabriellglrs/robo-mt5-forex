---
phase: 21-asset-deep-dive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/api/main.py, web-dashboard/package.json, web-dashboard/src/app/page.tsx, web-dashboard/src/components/LiveChart.tsx, web-dashboard/src/app/monitor/[symbol]/page.tsx]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths: 
    - "User can view historical candlestick data on the new monitor page"
    - "User can switch between M1 and M15 timeframes"
    - "User can see Fimathe Price Lines overlaid on the chart"
  artifacts: 
    - path: "web-dashboard/src/components/LiveChart.tsx"
      provides: "Lightweight Charts integration for rendering candlesticks"
    - path: "web-dashboard/src/app/monitor/[symbol]/page.tsx"
      provides: "Dedicated route for the asset deep dive"
    - path: "src/api/main.py"
      provides: "MT5 endpoint for fetching copy_rates_from_pos"
      exports: ["GET /api/chart/{symbol}"]
  key_links: 
    - from: "web-dashboard/src/app/monitor/[symbol]/page.tsx"
      to: "/api/chart"
      via: "fetch historical candles on load"
      pattern: "fetch.*api/chart"
---

<objective>
Build the Asset Deep Dive (Live Terminal) featuring TradingView Lightweight Charts and an API bridge for historical MT5 OHLC data.

Purpose: Transform the robot from a black-box execution engine to a professional trading terminal.
Output: New Next.js route `/monitor/[symbol]`, LiveChart component, and FastAPI endpoint for historical candles.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-21/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MT5 Chart API Endpoint</name>
  <files>src/api/main.py</files>
  <read_first>src/api/main.py</read_first>
  <action>Import `MetaTrader5` locally within a new endpoint `GET /api/chart/{symbol}`. Initialize connection just for the request, fetch `copy_rates_from_pos` (last 500 candles), and map them to `{time, open, high, low, close}` format. Support `tf` query parameter (`M1` or `M15`). Uninitialize safely or keep connection open depending on best practice for FastAPI+MT5.</action>
  <verify>curl to /api/chart/EURUSD returns JSON array of OHLC data</verify>
  <acceptance_criteria>
    - `src/api/main.py` contains `/api/chart/{symbol}` route
    - Route returns valid array with `{time, open, high, low, close}` properties
  </acceptance_criteria>
  <done>Endpoint returns formatted candle data</done>
</task>

<task type="auto">
  <name>Task 2: Install and configure Lightweight Charts</name>
  <files>web-dashboard/package.json, web-dashboard/src/components/LiveChart.tsx</files>
  <read_first>web-dashboard/package.json</read_first>
  <action>Add `lightweight-charts` to `web-dashboard/package.json`. Create `LiveChart.tsx` component that accepts OHLC data and Fimathe lines (A, B, SL, TP). Set chart layout to dark mode. Expose a `update(tick)` method or use React effects to update the last candle real-time based on `fimathe_runtime.json`'s live price.</action>
  <verify>npm run build passes with new component</verify>
  <acceptance_criteria>
    - `package.json` contains `lightweight-charts`
    - `LiveChart.tsx` exports a valid React component
  </acceptance_criteria>
  <done>Charts library is integrated and component is rendering</done>
</task>

<task type="auto">
  <name>Task 3: Create Asset Terminal Page & Routing</name>
  <files>web-dashboard/src/app/page.tsx, web-dashboard/src/app/monitor/[symbol]/page.tsx</files>
  <read_first>web-dashboard/src/app/page.tsx</read_first>
  <action>Create the `/monitor/[symbol]` dynamic route. Fetch initial OHLC data from `/api/chart` using M15 as default. Render `LiveChart` and a sidebar showing the 16 Fimathe rules based on `fimathe_runtime.json` state. Add a toggle for M1/M15 that re-fetches data. In `web-dashboard/src/app/page.tsx`, add a link/button to the Symbol card routing to this new page.</action>
  <verify>Navigate to /monitor/EURUSD without errors</verify>
  <acceptance_criteria>
    - `/monitor/[symbol]/page.tsx` exists and fetches from API
    - Symbol card in `page.tsx` has navigation button to the terminal
  </acceptance_criteria>
  <done>Asset terminal is functional and accessible from dashboard</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] curl http://localhost:8000/api/chart/EURUSD?tf=M1
- [ ] npm run build run successfully
- [ ] Visual verification of chart loading and reacting to live price
</verification>

<success_criteria>
- All tasks completed
- Backend endpoint successfully returns chart data
- Trading terminal UI works as expected
</success_criteria>

<output>
After completion, create `.planning/phases/21-asset-deep-dive/21-01-SUMMARY.md`
</output>
