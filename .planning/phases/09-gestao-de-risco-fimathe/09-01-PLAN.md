# Plan: Phase 09 - Gestao de Risco Fimathe

Finalize the risk management motor with precise stop placement, deterministic trailing, and 3% risk capping based on Balance.

## Proposed Changes

### 1. Risk Motor (Lot Sizing)
- **File**: `src/execution/risk.py`
- **Task**: 
  - Update `calculate_lot` to strictly use `AccountBalance`.
  - Ensure `risk_percentage` is capped at `min(user_config, 3.0)`.
  - Validate lot normalization (min_lot, lot_step).

### 2. Initial Stop (STI) Logic
- **File**: `src/execution/risk.py`
- **Task**:
  - Refine `calculate_prices` to correctly identify the "structural floor/ceiling".
  - For BUY: Initial SL = Bottom of Neutral Zone (or Trend Channel if aggressive) - Buffer.
  - Coordinate with `FimatheStateEngine` labels for level identification.

### 3. Trailing Engine (Proactive Tracking)
- **File**: `src/execution/fimathe_cycle.py`
- **Task**:
  - Replace "crossed back" logic with "reached and stabilized" logic.
  - Event 1: Price reaches 50% projection -> SL = Entry Price + Offset.
  - Event 2: Price reaches 100% projection -> SL = 50% Level of projection.
  - Add `reached_50` and `reached_100` state flags to avoid repeated moves.

### 4. Integration and Logging
- **File**: `src/main.py`
- **Task**:
  - Verify that `apply_trailing_management` correctly feeds the required levels from the position's entry context.
  - Add explicit logging for "FIM-010" events (Trailing adjustments).

## Verification Plan

### Automated Tests
- `tests/test_risk_manager.py`: Test `calculate_lot` with different account balances.
- `tests/test_fimathe_cycle.py`: Test trailing triggers (BE at 50%, Locking at 100%).

### Manual Verification
- Run the robot on a MT5 Demo account.
- Trigger a signal (manual or automatic).
- Observe the SL modification in the "Toolbox" when price hits the 50% and 100% projection lines.
