# Plan 10-01: Gestão de Configurações e Dashboard Base

Este plano foca na infraestrutura de configurações (`settings.json`) e na criação do esqueleto do dashboard Streamlit.

## Frontmatter
wave: 1
depends_on: []
files_modified:
  - src/main.py
  - config/settings.json
  - src/dashboard/app.py
  - src/dashboard/components/controls.py
autonomous: true

## Tasks

<task>
<read_first>
- src/main.py
- .streamlit/config.toml
</read_first>
<acceptance_criteria>
- config/settings.json existe com estrutura vǭlida.
- src/main.py lê settings.json sem erros ao iniciar.
- src/main.py possui loop que respeita 'running_state': True.
</acceptance_criteria>
<action>
1. Criar `config/settings.json` com campos: `risk_management` (percent, max_pos), `analysis` (symbols, history_years), e `running_state` (bool).
2. Modificar `src/main.py` para usar um flag de controle baseado em `settings.json["running_state"]`.
</action>
</task>

<task>
<read_first>
- src/main.py
</read_first>
<acceptance_criteria>
- src/dashboard/app.py é um script Streamlit vǭlido.
- Sidebar de src/dashboard/components/controls.py contém sliders para Risco e botões Start/Stop.
</acceptance_criteria>
<action>
1. Criar `src/dashboard/app.py` com layout básico de monitoramento.
2. Criar `src/dashboard/components/controls.py` com sliders de Risco e botão Start/Stop que salva no `config/settings.json`.
</action>
</task>

## Verification
- Rodar o robô (`python src/main.py`).
- Rodar a UI (`streamlit run src/dashboard/app.py`).
- Alterar o risco na UI e verificar se o robô percebe a mudança.
